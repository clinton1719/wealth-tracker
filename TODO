fix categoryIcon - in form SVGUnitTypes, backend etc











Try and use usememo() wherever possible.  - because of usememo - sorting is not done for profiles, accounts EventCounts, fix that
<Child onDelete={() => doThing(id)} />
const handleDelete = useCallback(() => doThing(id), [id]);
Use useCallback

